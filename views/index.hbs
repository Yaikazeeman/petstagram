<div class="feed">
    {{!-- {{#each post}}
        {{> post}}
    {{/each}} --}}
    {{#each posts}}
    {{!-- <div>
        <img src="{{this.profileImg}}">
        <p>{{this.username}}</p>
    </div> --}}
    <img src="/images/userPhotos/{{image}}">
    <div>
        <p>{{caption}}</p>
        <div>
            <button class="likeButton">like</button>
            <button class="commentButton">comment</button>
            {{!-- <p>Likes: {{///likes number}}</p> --}}
        </div>
    </div>
    {{/each}}
</div>